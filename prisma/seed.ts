// Seed data is now managed per-user via the /api/seed endpoint
// and auto-seeded on first login/signup.
// This file is kept for reference but is no longer used directly.
console.log("Seeding is now handled per-user via /api/seed endpoint.");
console.log("New users receive demo data automatically on first login.");
